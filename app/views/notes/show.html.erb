<h1>Note <%= @note.id %></h1>
<p>
	<b>Title:</b>
	<%= @note.title %><br />
</p>

<p>
	<b>Body:</b>
	<%= @note.body %><br />
</p>

<%= link_to 'Edit', edit_project_note_path(@note.project, @note) %> |
<%= link_to 'Back', project_path(@note.project) %>
